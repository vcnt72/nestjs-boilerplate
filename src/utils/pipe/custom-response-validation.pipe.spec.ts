import { CustomResponseValidationPipe } from './custom-response-validation.pipe';

describe('CustomResponseValidationPipe', () => {
  it('should be defined', () => {
    expect(new CustomResponseValidationPipe()).toBeDefined();
  });
});
